<ul class="product-list">
  {{#each products}}
  <li class="product-item" data-name="{{name}}">
    <h2 class="name">{{name}}</h2>
    <p class="price">Price: {{price}}</p>
    <p class="description">{{description}}</p>
  </li>
  {{/each}}
</ul>
